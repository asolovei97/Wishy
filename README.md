# Wishy — Платформа вішлістів 🎁

Wishy — це сучасна екосистема для створення та управління списками бажань (вішлістами). Система дозволяє користувачам додавати товари вручну або через посилання, автоматично витягуючи метадані (назву, ціну, зображення) з популярних маркетплейсів, а також ділитися списками з друзями та керувати бюджетом.

---

## 🏗 Технологічний стек

Проєкт реалізовано як **Monorepo** з використанням **Turborepo**.

### Core
*   **Monorepo Tools:** [Turborepo](https://turbo.build/)
*   **Package Manager:** [Bun](https://bun.sh/) (або npm/yarn/pnpm)

### Backend
*   **Framework:** Node.js (NestJS або Fastify)
*   **Database:** PostgreSQL
*   **ORM:** Prisma

### Frontend (Client)
*   **Framework:** Next.js (React)
*   **Language:** TypeScript
*   **Styling:** Tailwind CSS / CSS Modules

### Mobile
*   **Framework:** React Native (Expo)

### Admin Dashboard
*   **Framework:** React (Vite) або Next.js section

### Shared
*   **Validation:** Zod
*   **Types:** TypeScript Shared Intefaces

---

## 📂 Архітектура монорепозиторію

Структура папок організована для максимального перевикористання коду:

```plaintext
wishy/
├── apps/
│   ├── api/          # Backend API (NestJS/Express/Fastify)
│   ├── web/          # Next.js (Клієнтський застосунок)
│   ├── mobile/       # React Native (Expo)
│   └── admin/        # Панель керування (Admin Dashboard)
├── packages/
│   ├── ui/           # Спільні UI компоненти (React)
│   ├── config/       # Конфігурації (eslint, tsconfig, prettier)
│   ├── shared/       # Спільні типи, схеми валидації (Zod), утиліти
│   └── parser/       # Ізольований модуль для парсингу даних з маркетплейсів
└── package.json
```

---

## 🔑 Ключові модулі та функціонал

### A. Модуль парсингу (Wishy Parser)
Серце системи для автоматичного заповнення карток товарів.
*   **Flow:** User Link -> API -> Parser Service -> Metadata JSON -> API -> User Confirmation.
*   **Технології:** Puppeteer / Playwright (для динамічних сайтів), Cheerio (для статичних).
*   **Підтримка:** Amazon, Rozetka та інші популярні платформи.

### B. Модуль поширення (Share Service)
Інструменти для зручного шерингу списків.
*   **QR-коди:** Генерація унікальних QR для швидкого доступу.
*   **Deep Links:** Розумні посилання, що відкривають мобільний додаток на потрібному екрані.

### C. Фінансові сервіси та Бюджетування
*   **Калькулятор вартості:** Підрахунок загальної суми вішліста.
*   **Бронювання:** Гості можуть "бронювати" товари, щоб уникнути дублювання подарунків.
*   **Мультивалютність:** Автоматична конвертація цін через API курсів валют.

---

## 🗄 Схема бази даних (Основне)

Спрощена модель даних системи:

*   **User:** `ID`, `email`, `hash`, `profile_info`
*   **Wishlist:** `ID`, `title`, `owner_id`, `privacy` (public/private), `slug`
*   **Item:** `ID`, `wishlist_id`, `title`, `price`, `currency`, `image_url`, `original_url`, `status` (available/reserved)
*   **Reservation:** `ID`, `item_id`, `guest_name` (user_id if auth)

---

## 🚀 Roadmap

### Фаза 1: Foundation (MVP)
- [ ] Налаштування Turborepo, Bun, TypeScript configs.
- [ ] Базові типи та Zod-схеми в `packages/shared`.
- [ ] API: Auth, CRUD для вішлістів.
- [ ] Web: Базовий інтерфейс користувача.

### Фаза 2: Parsing & Intelligence
- [ ] Реалізація `packages/parser`.
- [ ] Інтеграція парсингу в API.
- [ ] Логіка бюджетування та калькуляції.

### Фаза 3: Mobile & Social
- [ ] Розробка React Native додатку.
- [ ] QR-коди, Public View сторінки.
- [ ] Система резервації подарунків.

### Фаза 4: Admin & Analytics
- [ ] Адмін-панель для моніторингу.
- [ ] Аналітика популярності товарів.

---

## 🛠️ Запуск проєкту

Встановити залежності:

```bash
bun install
```

Запустити всі додатки (dev mode):

```bash
bun run dev
```

Збірка проєкту:

```bash
bun run build
```
